version: 1.7.{build}

branches:
  only:
  - master
  - rel

image: Visual Studio 2019

install:
  - ps: (new-object Net.WebClient).DownloadString("https://raw.github.com/madskristensen/ExtensionScripts/master/AppVeyor/vsix.ps1") | iex

assembly_info:
  patch: true
  file: '**\AssemblyInfo.*'
  assembly_version: '{version}'
  assembly_file_version: '{version}'
  assembly_informational_version: '{version}'

before_build:
  - nuget restore
  - ps: Vsix-IncrementVsixVersion | Vsix-UpdateBuildVersion
  - ps: Vsix-TokenReplacement Kool.VsDiff\Vsix.cs 'VERSION = "([0-9\\.]+)"' 'VERSION = "{version}"'

build_script:
  - msbuild /p:configuration=Release /p:DeployExtension=false /p:ZipPackageCompressionLevel=normal /v:m

after_test:
  - ps: if ($env:APPVEYOR_REPO_BRANCH -eq 'master') { Vsix-PushArtifacts }
  - ps: if ($env:APPVEYOR_REPO_BRANCH -eq 'rel') { Vsix-PushArtifacts | Vsix-PublishToGallery }